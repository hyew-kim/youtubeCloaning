extends base.pug

block content
	div
		video(src="/" + video.fileUrl, autoplay, controls)
		p=video.description
		ul
		each tag in video.hashtags
			li=tag
		small=video.createAt
		
		br
		a(href=`${video.id}/edit`) Edit video &rarr;
		br
		a(href=`${video.id}/remove`) Delete video &rarr;